# Bootloaders

Um bootloader é um programa que carrega o sistema operacional.

## Bootloaders populares

* `GRUB`: O bootloader padrão na maioria das distribuições.
* `LILO`: Um bootloader mais antigo.
* `systemd-boot`: Um bootloader simples para sistemas UEFI.
