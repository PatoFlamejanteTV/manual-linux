# Ruby no Linux

Ruby é uma linguagem dinâmica e orientada a objetos.

## Instalação (RVM ou rbenv)

Recomendado usar um gerenciador de versões, mas via apt:

```bash
sudo apt install ruby-full
```

## Gems

Gems são pacotes Ruby.

```bash
sudo gem install bundler
```
