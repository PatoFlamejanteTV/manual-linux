# pwd

`pwd` (Print Working Directory) é um comando que mostra em qual diretório você está atualmente.

```bash
$ pwd
/home/quack/manual-linux
```

É útil quando você está "perdido" no terminal e não sabe onde está.
