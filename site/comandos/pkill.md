# pkill

Envia sinais para processos baseando-se no nome e outros atributos, em vez do PID.

```
╭─quack @ termux in ~
╰─❯ pkill firefox
```

Para enviar um sinal específico (ex: SIGKILL):

```
╭─quack @ termux in ~
╰─❯ pkill -9 chrome
```
