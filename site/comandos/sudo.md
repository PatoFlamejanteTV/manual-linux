# sudo

O `sudo` é o comando que te dá superpoderes. Ele permite que você execute
comandos como o usuário administrador (também chamado de "root"). É muito
útil para instalar programas ou mexer em arquivos do sistema, mas cuidado!
Com grandes poderes vêm grandes responsabilidades.

Exemplo de uso:

```bash
$ sudo apt update
```
