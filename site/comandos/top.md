# top

O `top` mostra o que está acontecendo no seu computador agora. Ele exibe
uma lista dos processos que estão rodando, quanto de memória e processador
eles estão gastando. É tipo o Gerenciador de Tarefas do Windows, mas no
terminal.

Exemplo de uso:

```bash
$ top
```

Para sair, aperte `q`.
