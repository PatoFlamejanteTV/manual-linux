# umount

O **umount** desmonta sistemas de arquivos.

## Desmontar pelo ponto de montagem

```bash
sudo umount /mnt/dados
```

## Desmontar forçadamente (Lazy)

Se o dispositivo estiver ocupado:

```bash
sudo umount -l /mnt/dados
```
